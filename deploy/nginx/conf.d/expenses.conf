server {
    listen       80;
    server_name  localhost;
    
    location / {
        root /usr/share/nginx/html;
    }
    
    location /api/ {
        rewrite    /api/(.+) /$1 break;
        proxy_pass http://api:9000;
    }
}
